name: 3. Deploy to Azure

on:
  push:
    branches:
      - github-actions-test

  # enables manual triggers
  workflow_dispatch:
    inputs:
      release_id:
        description: 'Release tag id to be deployed'
        required: true
        type: string

jobs:
  deploy:
    name: Deploy to Azure
    runs-on: ubuntu-latest
    needs: []
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build --platform linux/amd64 -t cliedl/electify:1.0.1 .

      - name: Push Docker Image
        run: |
          docker push cliedl/electify:1.0.1

      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Container Apps
        run: |
          az containerapp update \
            --name electify \
            --resource-group electify-app \
            --image cliedl/electify:1.0.1
